diff --git a/node_modules/expo-modules-autolinking/android/expo-gradle-plugin/expo-autolinking-plugin-shared/src/main/kotlin/expo/modules/plugin/AutolinkigCommandBuilder.kt b/node_modules/expo-modules-autolinking/android/expo-gradle-plugin/expo-autolinking-plugin-shared/src/main/kotlin/expo/modules/plugin/AutolinkigCommandBuilder.kt
index 2064ca4..fefe6c0 100644
--- a/node_modules/expo-modules-autolinking/android/expo-gradle-plugin/expo-autolinking-plugin-shared/src/main/kotlin/expo/modules/plugin/AutolinkigCommandBuilder.kt
+++ b/node_modules/expo-modules-autolinking/android/expo-gradle-plugin/expo-autolinking-plugin-shared/src/main/kotlin/expo/modules/plugin/AutolinkigCommandBuilder.kt
@@ -1,5 +1,7 @@
 package expo.modules.plugin
 
+import java.io.File
+
 /**
  * Builder for creating command to run using `expo-modules-autolinking`.
  */
@@ -8,7 +10,7 @@ class AutolinkingCommandBuilder {
    * Command for finding and running `expo-modules-autolinking`.
    */
   private val baseCommand = listOf(
-    "node",
+    resolveNodeExecutable(),
     "--no-warnings",
     "--eval",
     "require('expo/bin/autolinking')",
@@ -76,6 +78,25 @@ class AutolinkingCommandBuilder {
     return Os.windowsAwareCommandLine(command)
   }
 
+  private fun resolveNodeExecutable(): String {
+    val nodeBinary = System.getenv("NODE_BINARY")?.takeIf { it.isNotBlank() && File(it).canExecute() }
+    if (nodeBinary != null) {
+      return nodeBinary
+    }
+
+    val homebrewNode = File("/opt/homebrew/bin/node")
+    if (homebrewNode.canExecute()) {
+      return homebrewNode.absolutePath
+    }
+
+    val localNode = File("/usr/local/bin/node")
+    if (localNode.canExecute()) {
+      return localNode.absolutePath
+    }
+
+    return "node"
+  }
+
   companion object {
     const val IGNORE_PATHS_KEY = "ignore-paths"
     const val EXCLUDE_KEY = "exclude"
diff --git a/node_modules/expo-modules-autolinking/android/expo-gradle-plugin/expo-autolinking-settings-plugin/src/main/kotlin/expo/modules/plugin/ExpoAutolinkingSettingsExtension.kt b/node_modules/expo-modules-autolinking/android/expo-gradle-plugin/expo-autolinking-settings-plugin/src/main/kotlin/expo/modules/plugin/ExpoAutolinkingSettingsExtension.kt
index 4255d95..72f006e 100644
--- a/node_modules/expo-modules-autolinking/android/expo-gradle-plugin/expo-autolinking-settings-plugin/src/main/kotlin/expo/modules/plugin/ExpoAutolinkingSettingsExtension.kt
+++ b/node_modules/expo-modules-autolinking/android/expo-gradle-plugin/expo-autolinking-settings-plugin/src/main/kotlin/expo/modules/plugin/ExpoAutolinkingSettingsExtension.kt
@@ -57,7 +57,7 @@ open class ExpoAutolinkingSettingsExtension(
     File(
       settings.providers.exec { env ->
         env.workingDir(projectRoot)
-        env.commandLine("node", "--print", "require.resolve('@react-native/gradle-plugin/package.json', { paths: [require.resolve('react-native/package.json')] })")
+        env.commandLine(resolveNodeExecutable(), "--print", "require.resolve('@react-native/gradle-plugin/package.json', { paths: [require.resolve('react-native/package.json')] })")
       }.standardOutput.asText.get().trim(),
     ).parentFile
   }
@@ -69,7 +69,7 @@ open class ExpoAutolinkingSettingsExtension(
     File(
       settings.providers.exec { env ->
         env.workingDir(projectRoot)
-        env.commandLine("node", "--print", "require.resolve('react-native/package.json')")
+        env.commandLine(resolveNodeExecutable(), "--print", "require.resolve('react-native/package.json')")
       }.standardOutput.asText.get().trim(),
     ).parentFile
   }
@@ -142,4 +142,23 @@ open class ExpoAutolinkingSettingsExtension(
       }
     }
   }
+
+  private fun resolveNodeExecutable(): String {
+    val nodeBinary = System.getenv("NODE_BINARY")?.takeIf { it.isNotBlank() && File(it).canExecute() }
+    if (nodeBinary != null) {
+      return nodeBinary
+    }
+
+    val homebrewNode = File("/opt/homebrew/bin/node")
+    if (homebrewNode.canExecute()) {
+      return homebrewNode.absolutePath
+    }
+
+    val localNode = File("/usr/local/bin/node")
+    if (localNode.canExecute()) {
+      return localNode.absolutePath
+    }
+
+    return "node"
+  }
 }
diff --git a/node_modules/expo-modules-autolinking/android/expo-gradle-plugin/expo-autolinking-settings-plugin/src/main/kotlin/expo/modules/plugin/ExpoAutolinkingSettingsPlugin.kt b/node_modules/expo-modules-autolinking/android/expo-gradle-plugin/expo-autolinking-settings-plugin/src/main/kotlin/expo/modules/plugin/ExpoAutolinkingSettingsPlugin.kt
index 99ccc9f..bc28d34 100644
--- a/node_modules/expo-modules-autolinking/android/expo-gradle-plugin/expo-autolinking-settings-plugin/src/main/kotlin/expo/modules/plugin/ExpoAutolinkingSettingsPlugin.kt
+++ b/node_modules/expo-modules-autolinking/android/expo-gradle-plugin/expo-autolinking-settings-plugin/src/main/kotlin/expo/modules/plugin/ExpoAutolinkingSettingsPlugin.kt
@@ -36,7 +36,7 @@ open class ExpoAutolinkingSettingsPlugin : Plugin<Settings> {
     val expoModulesAutolinkingPath =
       settings.providers.exec { env ->
         env.workingDir(settings.rootDir)
-        env.commandLine("node", "--print", "require.resolve('expo-modules-autolinking/package.json', { paths: [require.resolve('expo/package.json')] })")
+        env.commandLine(resolveNodeExecutable(), "--print", "require.resolve('expo-modules-autolinking/package.json', { paths: [require.resolve('expo/package.json')] })")
       }.standardOutput.asText.get().trim()
 
     val expoAutolinkingDir = File(expoModulesAutolinkingPath).parentFile
@@ -46,4 +46,23 @@ open class ExpoAutolinkingSettingsPlugin : Plugin<Settings> {
       "android/expo-gradle-plugin"
     )
   }
+
+  private fun resolveNodeExecutable(): String {
+    val nodeBinary = System.getenv("NODE_BINARY")?.takeIf { it.isNotBlank() && File(it).canExecute() }
+    if (nodeBinary != null) {
+      return nodeBinary
+    }
+
+    val homebrewNode = File("/opt/homebrew/bin/node")
+    if (homebrewNode.canExecute()) {
+      return homebrewNode.absolutePath
+    }
+
+    val localNode = File("/usr/local/bin/node")
+    if (localNode.canExecute()) {
+      return localNode.absolutePath
+    }
+
+    return "node"
+  }
 }
diff --git a/node_modules/expo-modules-autolinking/scripts/android/autolinking_implementation.gradle b/node_modules/expo-modules-autolinking/scripts/android/autolinking_implementation.gradle
index 27f1fe9..613572f 100644
--- a/node_modules/expo-modules-autolinking/scripts/android/autolinking_implementation.gradle
+++ b/node_modules/expo-modules-autolinking/scripts/android/autolinking_implementation.gradle
@@ -181,8 +181,24 @@ class ExpoAutolinkingManager {
   }
 
   static private String[] convertOptionsToCommandArgs(String command, Map options) {
+    def nodeExecutable = System.getenv("NODE_BINARY")
+    if (nodeExecutable != null && !nodeExecutable.trim().isEmpty() && !new File(nodeExecutable).canExecute()) {
+      nodeExecutable = null
+    }
+    if (nodeExecutable == null || nodeExecutable.trim().isEmpty()) {
+      def homebrewNode = new File("/opt/homebrew/bin/node")
+      def localNode = new File("/usr/local/bin/node")
+      if (homebrewNode.canExecute()) {
+        nodeExecutable = homebrewNode.absolutePath
+      } else if (localNode.canExecute()) {
+        nodeExecutable = localNode.absolutePath
+      } else {
+        nodeExecutable = "node"
+      }
+    }
+
     String[] args = [
-      'node',
+      nodeExecutable,
       '--no-warnings',
       '--eval',
       'require(\'expo/bin/autolinking\')',
diff --git a/node_modules/expo-modules-autolinking/build/platforms/android/android.js b/node_modules/expo-modules-autolinking/build/platforms/android/android.js
index 5154abd..aee6855 100644
--- a/node_modules/expo-modules-autolinking/build/platforms/android/android.js
+++ b/node_modules/expo-modules-autolinking/build/platforms/android/android.js
@@ -161,7 +161,7 @@ function findAndroidModules(modules) {
     return classNames;
 }
 async function findAndroidPackagesAsync(modules) {
-    const classes = [];
+    const classes = new Set();
     const flattenedSourceDirList = [];
     for (const module of modules) {
         for (const project of module.projects ?? []) {
@@ -170,6 +170,9 @@ async function findAndroidPackagesAsync(modules) {
     }
     await Promise.all(flattenedSourceDirList.map(async (sourceDir) => {
         for await (const file of (0, utils_1.scanFilesRecursively)(sourceDir)) {
+            if (file.path.includes(`${path_1.default.sep}bin${path_1.default.sep}`)) {
+                continue;
+            }
             if (!file.name.endsWith('Package.java') && !file.name.endsWith('Package.kt')) {
                 continue;
             }
@@ -189,11 +192,11 @@ async function findAndroidPackagesAsync(modules) {
             const classPathMatches = fileContent.match(/^package ([\w.]+)\b/m);
             if (classPathMatches) {
                 const basename = path_1.default.basename(file.name, path_1.default.extname(file.name));
-                classes.push(`${classPathMatches[1]}.${basename}`);
+                classes.add(`${classPathMatches[1]}.${basename}`);
             }
         }
     }));
-    return classes.sort();
+    return Array.from(classes).sort();
 }
 /**
  * Converts the package name to Android's project name.
diff --git a/node_modules/expo-modules-autolinking/src/platforms/android/android.ts b/node_modules/expo-modules-autolinking/src/platforms/android/android.ts
index 5d145d2..122ca21 100644
--- a/node_modules/expo-modules-autolinking/src/platforms/android/android.ts
+++ b/node_modules/expo-modules-autolinking/src/platforms/android/android.ts
@@ -203,7 +203,7 @@ function findAndroidModules(modules: ModuleDescriptorAndroid[]): string[] {
 }
 
 async function findAndroidPackagesAsync(modules: ModuleDescriptorAndroid[]): Promise<string[]> {
-  const classes: string[] = [];
+  const classes = new Set<string>();
 
   const flattenedSourceDirList: string[] = [];
   for (const module of modules) {
@@ -215,6 +215,10 @@ async function findAndroidPackagesAsync(modules: ModuleDescriptorAndroid[]): Pro
   await Promise.all(
     flattenedSourceDirList.map(async (sourceDir) => {
       for await (const file of scanFilesRecursively(sourceDir)) {
+        if (file.path.includes(`${path.sep}bin${path.sep}`)) {
+          continue;
+        }
+
         if (!file.name.endsWith('Package.java') && !file.name.endsWith('Package.kt')) {
           continue;
         }
@@ -238,12 +242,12 @@ async function findAndroidPackagesAsync(modules: ModuleDescriptorAndroid[]): Pro
 
         if (classPathMatches) {
           const basename = path.basename(file.name, path.extname(file.name));
-          classes.push(`${classPathMatches[1]}.${basename}`);
+          classes.add(`${classPathMatches[1]}.${basename}`);
         }
       }
     })
   );
-  return classes.sort();
+  return Array.from(classes).sort();
 }
 
 /**
