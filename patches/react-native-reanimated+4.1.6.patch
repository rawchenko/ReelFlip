diff --git a/node_modules/react-native-reanimated/android/build.gradle b/node_modules/react-native-reanimated/android/build.gradle
index 6b73a69..6755049 100644
--- a/node_modules/react-native-reanimated/android/build.gradle
+++ b/node_modules/react-native-reanimated/android/build.gradle
@@ -20,17 +20,48 @@ def isNewArchitectureEnabled() {
     return project.hasProperty("newArchEnabled") && project.newArchEnabled == "true"
 }
 
+def nodeExecutable = System.getenv("NODE_BINARY")
+if (nodeExecutable != null && !nodeExecutable.trim().isEmpty() && !new File(nodeExecutable).canExecute()) {
+    nodeExecutable = null
+}
+if (nodeExecutable == null || nodeExecutable.trim().isEmpty()) {
+    def homebrewNode = new File("/opt/homebrew/bin/node")
+    def localNode = new File("/usr/local/bin/node")
+    if (homebrewNode.canExecute()) {
+        nodeExecutable = homebrewNode.absolutePath
+    } else if (localNode.canExecute()) {
+        nodeExecutable = localNode.absolutePath
+    } else {
+        nodeExecutable = "node"
+    }
+}
+
 def resolveReactNativeDirectory() {
     def reactNativeLocation = safeAppExtGet("REACT_NATIVE_NODE_MODULES_DIR", null)
     if (reactNativeLocation != null) {
         return file(reactNativeLocation)
     }
+    def nodeExec = System.getenv("NODE_BINARY")
+    if (nodeExec != null && !nodeExec.trim().isEmpty() && !new File(nodeExec).canExecute()) {
+        nodeExec = null
+    }
+    if (nodeExec == null || nodeExec.trim().isEmpty()) {
+        def homebrewNode = new File("/opt/homebrew/bin/node")
+        def localNode = new File("/usr/local/bin/node")
+        if (homebrewNode.canExecute()) {
+            nodeExec = homebrewNode.absolutePath
+        } else if (localNode.canExecute()) {
+            nodeExec = localNode.absolutePath
+        } else {
+            nodeExec = "node"
+        }
+    }
 
     // Fallback to node resolver for custom directory structures like monorepos.
     def reactNativePackage = file(
         providers.exec {
             workingDir(rootDir)
-            commandLine("node", "--print", "require.resolve('react-native/package.json')")
+            commandLine(nodeExec, "--print", "require.resolve('react-native/package.json')")
         }.standardOutput.asText.get().trim()
     )
     if (reactNativePackage.exists()) {
@@ -47,12 +78,27 @@ def resolveReactNativeWorkletsDirectory() {
     if (reactNativeWorkletsLocation != null) {
         return file(reactNativeWorkletsLocation)
     }
+    def nodeExec = System.getenv("NODE_BINARY")
+    if (nodeExec != null && !nodeExec.trim().isEmpty() && !new File(nodeExec).canExecute()) {
+        nodeExec = null
+    }
+    if (nodeExec == null || nodeExec.trim().isEmpty()) {
+        def homebrewNode = new File("/opt/homebrew/bin/node")
+        def localNode = new File("/usr/local/bin/node")
+        if (homebrewNode.canExecute()) {
+            nodeExec = homebrewNode.absolutePath
+        } else if (localNode.canExecute()) {
+            nodeExec = localNode.absolutePath
+        } else {
+            nodeExec = "node"
+        }
+    }
 
     // Fallback to node resolver for custom directory structures like monorepos.
     def reactNativeWorkletsPackage = file(
         providers.exec {
             workingDir(rootDir)
-            commandLine("node", "--print", "require.resolve('react-native-worklets/package.json')")
+            commandLine(nodeExec, "--print", "require.resolve('react-native-worklets/package.json')")
         }.standardOutput.asText.get().trim()
     )
     if (reactNativeWorkletsPackage.exists()) {
@@ -127,7 +173,7 @@ def REANIMATED_PROFILING = safeAppExtGet("enableReanimatedProfiling", false)
 def REANIMATED_FEATURE_FLAGS = getReanimatedStaticFeatureFlags()
 
 // Set version for prefab
-version REANIMATED_VERSION
+version = REANIMATED_VERSION
 
 def reanimatedPrefabHeadersDir = project.file("$buildDir/prefab-headers/reanimated")
 
@@ -159,32 +205,32 @@ apply plugin: "de.undercouch.download"
 android {
     compileSdkVersion safeExtGet("compileSdkVersion", 34)
 
-    namespace "com.swmansion.reanimated"
+    namespace = "com.swmansion.reanimated"
 
     if (rootProject.hasProperty("ndkPath")) {
         ndkPath rootProject.ext.ndkPath
     }
     if (rootProject.hasProperty("ndkVersion")) {
-        ndkVersion rootProject.ext.ndkVersion
+        ndkVersion = rootProject.ext.ndkVersion
     }
 
     buildFeatures {
-        prefab true
-        prefabPublishing true
-        buildConfig true
+        prefab = true
+        prefabPublishing = true
+        buildConfig = true
     }
 
     prefab {
         reanimated {
-            headers reanimatedPrefabHeadersDir.absolutePath
+            headers = reanimatedPrefabHeadersDir.absolutePath
         }
     }
 
     defaultConfig {
         minSdkVersion safeExtGet("minSdkVersion", 23)
         targetSdkVersion safeExtGet("targetSdkVersion", 34)
-        versionCode 1
-        versionName REANIMATED_VERSION
+        versionCode = 1
+        versionName = REANIMATED_VERSION
 
         buildConfigField("boolean", "REANIMATED_PROFILING", REANIMATED_PROFILING.toString())
         buildConfigField("String", "REANIMATED_VERSION_JAVA", "\"${REANIMATED_VERSION}\"")
@@ -225,7 +271,7 @@ android {
         }
     }
     lintOptions {
-        abortOnError false
+        abortOnError = false
     }
     packagingOptions {
         // For some reason gradle only complains about the duplicated version of librrc_root and libreact_render libraries
@@ -276,9 +322,10 @@ android {
     }
 }
 
+def nodeExecForVersionValidation = nodeExecutable
 def validateReactNativeVersionResult = providers.exec {
     workingDir(projectDir.path)
-    commandLine("node", "./../scripts/validate-react-native-version.js", REACT_NATIVE_VERSION.toString())
+    commandLine(nodeExecForVersionValidation, "./../scripts/validate-react-native-version.js", REACT_NATIVE_VERSION.toString())
     ignoreExitValue = true
 }
 
@@ -301,9 +348,10 @@ task assertNewArchitectureEnabledTask {
 
 preBuild.dependsOn(assertNewArchitectureEnabledTask)
 
+def nodeExecForWorkletsValidation = nodeExecutable
 def validateWorkletsBuildResult = providers.exec {
     workingDir(projectDir.path)
-    commandLine("node", "./../scripts/validate-worklets-build.js")
+    commandLine(nodeExecForWorkletsValidation, "./../scripts/validate-worklets-build.js")
     ignoreExitValue = true
 }
 
