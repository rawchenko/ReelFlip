diff --git a/node_modules/@react-native/gradle-plugin/react-native-gradle-plugin/src/main/kotlin/com/facebook/react/ReactExtension.kt b/node_modules/@react-native/gradle-plugin/react-native-gradle-plugin/src/main/kotlin/com/facebook/react/ReactExtension.kt
index 2e89b8c..81fa11e 100644
--- a/node_modules/@react-native/gradle-plugin/react-native-gradle-plugin/src/main/kotlin/com/facebook/react/ReactExtension.kt
+++ b/node_modules/@react-native/gradle-plugin/react-native-gradle-plugin/src/main/kotlin/com/facebook/react/ReactExtension.kt
@@ -21,6 +21,25 @@ abstract class ReactExtension @Inject constructor(val project: Project) {
 
   private val objects = project.objects
 
+  private fun defaultNodeExecutable(): String {
+    val nodeBinary = System.getenv("NODE_BINARY")?.takeIf { it.isNotBlank() && File(it).canExecute() }
+    if (nodeBinary != null) {
+      return nodeBinary
+    }
+
+    val homebrewNode = File("/opt/homebrew/bin/node")
+    if (homebrewNode.canExecute()) {
+      return homebrewNode.absolutePath
+    }
+
+    val localNode = File("/usr/local/bin/node")
+    if (localNode.canExecute()) {
+      return localNode.absolutePath
+    }
+
+    return "node"
+  }
+
   /**
    * The path to the root of your project. This is the path to where the `package.json` lives. All
    * the CLI commands will be invoked from this folder as working directory.
@@ -56,7 +75,7 @@ abstract class ReactExtension @Inject constructor(val project: Project) {
    * installed and configured in your $PATH. Default: ["node"]
    */
   val nodeExecutableAndArgs: ListProperty<String> =
-      objects.listProperty(String::class.java).convention(listOf("node"))
+      objects.listProperty(String::class.java).convention(listOf(defaultNodeExecutable()))
 
   /** The command to use to invoke bundle. Default is `bundle` and will be invoked on [root]. */
   val bundleCommand: Property<String> = objects.property(String::class.java).convention("bundle")
diff --git a/node_modules/@react-native/gradle-plugin/react-native-gradle-plugin/src/main/kotlin/com/facebook/react/internal/PrivateReactExtension.kt b/node_modules/@react-native/gradle-plugin/react-native-gradle-plugin/src/main/kotlin/com/facebook/react/internal/PrivateReactExtension.kt
index 31795b3..cd0ded2 100644
--- a/node_modules/@react-native/gradle-plugin/react-native-gradle-plugin/src/main/kotlin/com/facebook/react/internal/PrivateReactExtension.kt
+++ b/node_modules/@react-native/gradle-plugin/react-native-gradle-plugin/src/main/kotlin/com/facebook/react/internal/PrivateReactExtension.kt
@@ -7,6 +7,7 @@
 
 package com.facebook.react.internal
 
+import java.io.File
 import javax.inject.Inject
 import org.gradle.api.Project
 import org.gradle.api.file.DirectoryProperty
@@ -25,6 +26,25 @@ abstract class PrivateReactExtension @Inject constructor(project: Project) {
 
   private val objects = project.objects
 
+  private fun defaultNodeExecutable(): String {
+    val nodeBinary = System.getenv("NODE_BINARY")?.takeIf { it.isNotBlank() && File(it).canExecute() }
+    if (nodeBinary != null) {
+      return nodeBinary
+    }
+
+    val homebrewNode = File("/opt/homebrew/bin/node")
+    if (homebrewNode.canExecute()) {
+      return homebrewNode.absolutePath
+    }
+
+    val localNode = File("/usr/local/bin/node")
+    if (localNode.canExecute()) {
+      return localNode.absolutePath
+    }
+
+    return "node"
+  }
+
   val root: DirectoryProperty =
       objects
           .directoryProperty()
@@ -50,7 +70,7 @@ abstract class PrivateReactExtension @Inject constructor(project: Project) {
       objects.directoryProperty().convention(root.dir("node_modules/react-native"))
 
   val nodeExecutableAndArgs: ListProperty<String> =
-      objects.listProperty(String::class.java).convention(listOf("node"))
+      objects.listProperty(String::class.java).convention(listOf(defaultNodeExecutable()))
 
   val codegenDir: DirectoryProperty =
       objects.directoryProperty().convention(root.dir("node_modules/@react-native/codegen"))
diff --git a/node_modules/@react-native/gradle-plugin/react-native-gradle-plugin/src/main/kotlin/com/facebook/react/utils/PathUtils.kt b/node_modules/@react-native/gradle-plugin/react-native-gradle-plugin/src/main/kotlin/com/facebook/react/utils/PathUtils.kt
index 0081b31..5f20d4f 100644
--- a/node_modules/@react-native/gradle-plugin/react-native-gradle-plugin/src/main/kotlin/com/facebook/react/utils/PathUtils.kt
+++ b/node_modules/@react-native/gradle-plugin/react-native-gradle-plugin/src/main/kotlin/com/facebook/react/utils/PathUtils.kt
@@ -81,7 +81,7 @@ private fun detectCliFile(reactNativeRoot: File, preconfiguredCliFile: File?): F
   val nodeProcess =
       Runtime.getRuntime()
           .exec(
-              arrayOf("node", "--print", "require.resolve('react-native/cli');"),
+              arrayOf(resolveNodeExecutable(), "--print", "require.resolve('react-native/cli');"),
               emptyArray(),
               reactNativeRoot)
 
@@ -111,6 +111,25 @@ private fun detectCliFile(reactNativeRoot: File, preconfiguredCliFile: File?): F
           .trimIndent())
 }
 
+private fun resolveNodeExecutable(): String {
+  val nodeBinary = System.getenv("NODE_BINARY")?.takeIf { it.isNotBlank() && File(it).canExecute() }
+  if (nodeBinary != null) {
+    return nodeBinary
+  }
+
+  val homebrewNode = File("/opt/homebrew/bin/node")
+  if (homebrewNode.canExecute()) {
+    return homebrewNode.absolutePath
+  }
+
+  val localNode = File("/usr/local/bin/node")
+  if (localNode.canExecute()) {
+    return localNode.absolutePath
+  }
+
+  return "node"
+}
+
 /**
  * Computes the `hermesc` command location. The Algo follows this order:
  * 1. The path provided by the `hermesCommand` config in the `react` Gradle extension
